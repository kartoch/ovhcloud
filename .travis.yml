language: python
os:
#  - osx # does not work at the present time: travis cannot install python (?!)
  - linux
python:
  - "2.7"
  - "3.4"
  - "3.5"
  - "3.6"
install:
  pip install .
script:
  python setup.py test
branches:
  only:
    - master